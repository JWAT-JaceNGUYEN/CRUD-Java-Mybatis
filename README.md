# API-Mybatis

RESTful API demo sử dụng **Spring Boot + MyBatis + PostgreSQL**  
Dự án phục vụ mục đích **học MyBatis**, thao tác CRUD và test bằng **Postman** (không có frontend).

---

## 🚀 Tech Stack

- Java 17
- Spring Boot 3.x
- MyBatis
- PostgreSQL
- Gradle (Kotlin DSL)
- Lombok
- Postman (test API)

---

# Run Application

Using Gradle Wrapper:

./gradlew bootRun

Or build and run:

./gradlew build -x test
java -jar build/libs/API-Mybatis-0.0.1-SNAPSHOT.jar
